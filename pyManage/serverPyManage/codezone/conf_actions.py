#Auteur --> aiglematth
#But    --> Constantes qu'utilise le fichier actions.py

#Imports
from os.path import join, pardir, abspath, curdir

name = ""
PATH_TO_SCRIPTS = join(abspath(name), "bash_scripts") # Vous pouvez editer ce path vers l'endroit ouv vous stockez vos scripts


TIMEOUT = 5 # Le timeout avant de considerer que l'exe est un echec
